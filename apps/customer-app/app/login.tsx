import { useState } from "react";
import { Alert,Pressable,StyleSheet,Text,TextInput,View } from "react-native";
import { router } from "expo-router";
import { api,setToken } from "../src/api/client";
export default function Login(){const[mobile,setMobile]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const submit=async()=>{if(!mobile||!password)return Alert.alert('Login','Mobile और password भरें।');setBusy(true);try{const r=await api<any>('/auth/login',{method:'POST',body:{mobile,password},skipAuth:true});if(r?.token){await setToken(r.token);router.replace('/')}else Alert.alert('Login','Login response में token नहीं मिला।')}catch(e:any){Alert.alert('Login failed',e.message)}finally{setBusy(false)}};return <View style={s.page}><Text style={s.title}>Customer Login</Text><TextInput value={mobile} onChangeText={setMobile} keyboardType="phone-pad" placeholder="Mobile number" style={s.input}/><TextInput value={password} onChangeText={setPassword} secureTextEntry placeholder="Password" style={s.input}/><Pressable disabled={busy} style={s.btn} onPress={submit}><Text style={s.btnText}>{busy?'Please wait…':'Login'}</Text></Pressable><Pressable onPress={()=>router.push('/register')}><Text style={s.link}>नया account बनाएं</Text></Pressable></View>}
const s=StyleSheet.create({page:{flex:1,padding:24,justifyContent:'center',gap:14,backgroundColor:'#f7f7f5'},title:{fontSize:28,fontWeight:'800'},input:{backgroundColor:'#fff',borderWidth:1,borderColor:'#ddd',borderRadius:10,padding:13},btn:{padding:14,borderRadius:10,backgroundColor:'#72ff55',alignItems:'center'},btnText:{fontWeight:'800'},link:{textAlign:'center',fontWeight:'700',padding:10}});
