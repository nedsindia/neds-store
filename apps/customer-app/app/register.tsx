import { useState } from "react";
import { Alert,Pressable,ScrollView,StyleSheet,Text,TextInput } from "react-native";
import { router } from "expo-router";
import { api,setToken } from "../src/api/client";
export default function Register(){const[name,setName]=useState('');const[mobile,setMobile]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const submit=async()=>{if(!name||!mobile||!password)return Alert.alert('Register','Name, mobile और password भरें।');setBusy(true);try{const r=await api<any>('/auth/register',{method:'POST',body:{name,mobile,password},skipAuth:true});if(r?.token){await setToken(r.token);router.replace('/')}else router.replace('/login')}catch(e:any){Alert.alert('Registration failed',e.message)}finally{setBusy(false)}};return <ScrollView contentContainerStyle={s.page}><Text style={s.title}>Create Customer Account</Text><TextInput value={name} onChangeText={setName} placeholder="Full name" style={s.input}/><TextInput value={mobile} onChangeText={setMobile} keyboardType="phone-pad" placeholder="Mobile number" style={s.input}/><TextInput value={password} onChangeText={setPassword} secureTextEntry placeholder="Password" style={s.input}/><Pressable disabled={busy} style={s.btn} onPress={submit}><Text style={s.btnText}>{busy?'Creating…':'Register'}</Text></Pressable><Pressable onPress={()=>router.replace('/login')}><Text style={s.link}>पहले से account है? Login</Text></Pressable></ScrollView>}
const s=StyleSheet.create({page:{flexGrow:1,padding:24,justifyContent:'center',gap:14,backgroundColor:'#f7f7f5'},title:{fontSize:28,fontWeight:'800'},input:{backgroundColor:'#fff',borderWidth:1,borderColor:'#ddd',borderRadius:10,padding:13},btn:{padding:14,borderRadius:10,backgroundColor:'#72ff55',alignItems:'center'},btnText:{fontWeight:'800'},link:{textAlign:'center',fontWeight:'700',padding:10}});
